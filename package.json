{
  "name": "signscout",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SignScout — open house sign placement, with paid agent & enterprise plans.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^16.8.0"
  }
}
